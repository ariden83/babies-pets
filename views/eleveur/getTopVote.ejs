<div class="col-xs-12">
  <h3 class="ttl-h3 bd-grey mg-bottom-20 txt-up mg-top-20">Vous les avez aimé !!!</h3>
</div>
<div class="col-xs-12 col-sm-6"><% if (data.length) { const e = data[0]; %>
  <div class="row">
    <div class="col-xs-4 text-center">
      <span class="txt-rose" style="font-size: 67px"><i class="fas fa-heart"></i></span>
      <p><b><%- (e.details.vote || 0) %> votes</b></p>
    </div>
    <div class="col-xs-8">
    <h4 class="txt-one-up mg-bottom-20">
      <a target="_blank" href="/mon-compte/profil/<%- (e.url || '') %>" title="<%- (e.details.titre || e.pseudo || e.nom || 'Titre non renseigné') %>">
        <%- (e.details.titre || e.pseudo || e.nom || 'Titre non renseigné') %>
      </a>
    </h4>
    <p class="txt-justify"><%- (e.details.description || '') %></p>
    </div>
  </div>
<% } %>
</div>
<div class="col-xs-12 col-sm-6">
<% let j = 0; if (data.length) {
  data.forEach(e => { j++;
  if (j === 1) {return;}
  if (j >= 4) { %>
</div>
<div class="col-xs-12 col-sm-6">
  <% } %>
  <div class="row mg-bottom-20">
    <div class="col-xs-2 text-center">
      <span class="txt-rose" style="font-size: 28px">
        <i class="fas fa-heart"></i>
      </span>
    </div>
    <div class="col-xs-10">
      <p class="txt-one-up txt-bold mg-top-10">
        <a target="_blank" href="/mon-compte/profil/<%- (e.url || '') %>" title="<%- (e.details.titre || e.pseudo || e.nom || 'Titre non renseigné') %>">
          <%- (e.details.titre || e.pseudo || e.nom || 'Titre non renseigné') %>
        </a> - <b><%- (e.details.vote || 0) %> votes</b>
      </p>
    </div>
    <div class="col-xs-12">
      <p><%- (e.details.description || '') %></p>
    </div>
  </div>
  <% }); } %>
</div>
